import {Panel} from "@vkontakte/vkui";

const Calendar = ({id}) => {
  return (
    <Panel id={id}>
      <div className='w-screen h-screen bg-white'>Calendar</div>
    </Panel>
  );
};

export default Calendar;